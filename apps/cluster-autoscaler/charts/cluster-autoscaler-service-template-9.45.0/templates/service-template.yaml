apiVersion: k0rdent.mirantis.com/v1alpha1
kind: ServiceTemplate
metadata:
  name: cluster-autoscaler-9-45-0
  annotations:
    helm.sh/resource-policy: keep
spec:
  helm:
    chartSpec:
      chart: cluster-autoscaler
      version: 9.45.0
      interval: 10m0s
      sourceRef:
        kind: HelmRepository
        name: k0rdent-catalog 