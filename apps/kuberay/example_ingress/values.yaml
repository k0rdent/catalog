traefik:
  traefik:
    deployment:
      kind: DaemonSet
    ports:
      web:
        port: 8000
        hostPort: 80
      websecure:
        port: 8443
        hostPort: 443

ray-cluster:
  ray-cluster:
    head:
      service:
        type: ClusterIP
        ports:
          - name: dashboard
            port: 8265
            targetPort: 8265
