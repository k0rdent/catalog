dapr-dashboard:
  dapr-dashboard:
    ingress:
      enabled: true
      host: ''

traefik:
  traefik:
    deployment:
      kind: DaemonSet
    ports:
      web:
        port: 8000
        hostPort: 80
      websecure:
        port: 8443
        hostPort: 443
