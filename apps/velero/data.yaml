tags:
  - Backup and Recovery
title: "Velero"
summary: "Velero is the CNCF standard for Kubernetes backup, restore, and migration, capturing cluster resources and persistent volume snapshots to object storage and replaying them on demand. It supports scheduled backups with configurable retention, namespace-scoped or cluster-wide scope, and hooks for pre/post-backup commands — making it essential for protecting the stateful components of AI platforms such as vector databases, model registries, and pipeline metadata stores."
logo: "https://cdn-images-1.medium.com/max/1600/1*-9mb3AKnKdcL_QD3CMnthQ.png"
created: "2025-03-12T08:16:55Z"
github_repo: vmware-tanzu/velero
description: |
    Velero is an open source tool to safely backup and restore, perform disaster recovery,
    and migrate Kubernetes cluster resources and persistent volumes.

why_in_catalog: "Kubernetes cluster state — etcd contents, persistent volumes, and custom resources — is not automatically backed up. A misconfigured deployment, a corrupted operator, or a failed upgrade can leave a platform team with a cluster that needs to be rebuilt from scratch, taking an AI platform offline for hours. Velero captures cluster resources and persistent volume snapshots to object storage on a schedule and replays them on demand, turning a potential multi-day recovery into a predictable restore procedure with a defined RTO."

doc_link: https://vmware-tanzu.github.io/helm-charts/

# test settings
test_deploy_chart: false
test_wait_for_pods: "node-agent-"

validated_amd64: 'y'
validated_aws: 'y'
validated_azure: 'y'
validated_arm64: 'y' # aws t4g.medium
validated_local: 'y'
