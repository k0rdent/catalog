apiVersion: v1
kind: Service
metadata:
  name: {{ include "catalog-web.fullname" . }}
  labels:
    {{- include "catalog-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "catalog-web.selectorLabels" . | nindent 4 }}
